import Spinner from '../../../UI/Spinner/Spinner';

import './AdminListSpinner.scss';

const AdminListSpinner = () => (
  <div className='admin-list-spinner'>
    <Spinner type='small' />
  </div>
);

export default AdminListSpinner;
